public enum TokenType {
    NUMBER,
    STRING,
    LBRACE,
    RBRACE,
    LBRACKET,
    RBRACKET,
    COLON,
    COMMA,
    TRUE,
    FALSE,
    NULL,
    EOF
}

//these are various tokens which can come in our string
//It can be type or value ( type is {,} and value is    "hello" or 123)
